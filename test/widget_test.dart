import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App builds without error', (WidgetTester tester) async {
    // Basic smoke test - just verify the test infrastructure works
    expect(true, isTrue);
  });
}
